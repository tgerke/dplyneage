% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sqlglot_utils.R
\name{extract_lineage}
\alias{extract_lineage}
\title{Extract column lineage from a dplyr pipeline or SQL query}
\usage{
extract_lineage(
  sql,
  dialect = "duckdb",
  schema = NULL,
  show_sql = FALSE,
  engine = c("auto", "sqlglot", "r")
)
}
\arguments{
\item{sql}{A dbplyr lazy table (\code{tbl_lazy}) or a single SQL query string.
Lazy tables are analyzed directly from their lazy query tree (the SQL
recorded in \code{metadata} still comes from \code{\link[dbplyr:sql_render]{dbplyr::sql_render()}}); when
one is handled by the sqlglot engine instead, its database connection
is used to harvest table schemas automatically.}

\item{dialect}{SQL dialect the query is written in, e.g. \code{"duckdb"}
(the default), \code{"postgres"}, \code{"mysql"}, \code{"snowflake"}, \code{"bigquery"}.
Any dialect sqlglot understands works here.}

\item{schema}{Optional table schema used by the sqlglot engine to
attribute unqualified columns to the right table and to expand
\verb{SELECT *}: a named list mapping table names to character vectors of
column names, e.g. \code{list(orders = c("order_id", "amount"))}. Only
relevant for SQL strings — the R engine reads exact provenance from
the lazy query tree, and a lazy table that falls back to sqlglot
harvests its schema from the database connection automatically.}

\item{show_sql}{If \code{TRUE}, print the SQL being analyzed. Useful for
seeing what dbplyr generated from your pipeline. Default: \code{FALSE}.}

\item{engine}{Which lineage engine to use. \code{"auto"} (the default) uses
the pure-R engine for lazy tables when dbplyr (>= 2.5.0) is installed,
falling back to sqlglot for SQL strings or unsupported constructs.
\code{"r"} forces the pure-R engine and errors on anything it cannot trace.
\code{"sqlglot"} always renders to SQL and analyzes with sqlglot.}
}
\value{
A list with \code{nodes} and \code{edges} ready to pass to
\code{\link[=lineage_flow]{lineage_flow()}}, plus \code{metadata} recording the analyzed SQL, the
dialect, the engine used, and node/edge counts.
}
\description{
\code{extract_lineage()} traces every output column of a query back to the
source table columns it was computed from. Pipe a dbplyr lazy table
straight into it, or pass a SQL string. Aliases, CTEs, subqueries, set
operations like \code{UNION}, and multi-source expressions such as
\code{COALESCE(a.x, b.x)} all resolve to their true source columns.
}
\details{
Two engines are available. dbplyr lazy tables are analyzed by a pure-R
fast path that walks the pipeline's lazy query tree directly — no Python
required. SQL strings are analyzed by
\href{https://github.com/tobymao/sqlglot}{sqlglot}'s lineage engine via
reticulate (a Suggests dependency: install reticulate to enable this
engine; sqlglot itself is provisioned automatically). If a pipeline uses
a construct the R engine cannot trace (e.g. raw SQL injected with
\code{dbplyr::sql()}), it falls back to sqlglot automatically.

Both engines trace select-list lineage: columns used only in \code{filter()},
join conditions, or \code{arrange()} do not create lineage edges.
}
\examples{
\dontshow{if (dplyneage::has_sqlglot()) withAutoprint(\{ # examplesIf}
# Raw SQL: qualified columns resolve on their own
extract_lineage("SELECT c.id, c.name FROM customers c") |>
  lineage_flow()

# Supply a schema so unqualified columns attribute to the right table
# and SELECT * expands
extract_lineage(
  "SELECT c.name, order_date FROM customers c
   JOIN orders o ON c.id = o.customer_id",
  schema = list(
    customers = c("id", "name"),
    orders = c("customer_id", "order_date")
  )
)
\dontshow{\}) # examplesIf}
\dontshow{if (requireNamespace("dplyr", quietly = TRUE) && requireNamespace("dbplyr", quietly = TRUE) && requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# dbplyr pipelines: pipe straight in; the pure-R engine reads exact
# provenance from the pipeline itself, no Python needed
library(dplyr)

con <- DBI::dbConnect(duckdb::duckdb())
DBI::dbWriteTable(con, "customers", data.frame(id = 1, name = "a"))
DBI::dbWriteTable(con, "orders", data.frame(customer_id = 1, amount = 10))

tbl(con, "customers") |>
  left_join(tbl(con, "orders"), by = c("id" = "customer_id")) |>
  group_by(id, name) |>
  summarise(total_spent = sum(amount, na.rm = TRUE), .groups = "drop") |>
  extract_lineage() |>
  lineage_flow()

DBI::dbDisconnect(con)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=lineage_flow]{lineage_flow()}} to render the result;
\code{vignette("getting-started")} for a tour from simple pipelines to
CTEs and multi-source columns.
}
