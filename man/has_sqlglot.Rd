% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz.R
\name{has_sqlglot}
\alias{has_sqlglot}
\title{Is the Python sqlglot dependency available?}
\usage{
has_sqlglot()
}
\value{
\code{TRUE} if sqlglot can be loaded, \code{FALSE} otherwise
}
\description{
Python is only involved when \code{\link[=extract_lineage]{extract_lineage()}} analyzes raw SQL
strings or duckplyr frames (or falls back to sqlglot for a dbplyr
pipeline it cannot trace in R); dbplyr, dtplyr, and arrow pipelines
are analyzed by pure-R engines. The sqlglot engine
needs the reticulate package (a Suggests dependency; install it with
\code{install.packages("reticulate")}); dplyneage then declares its sqlglot
dependency via \code{\link[reticulate:py_require]{reticulate::py_require()}}, so sqlglot itself is
provisioned automatically the first time it is needed. Use this to
check availability, or to gate code that needs the sqlglot engine
(examples, vignette chunks, Shiny apps). Returns \code{FALSE} when
reticulate is not installed. Note that calling it may initialize
Python.
}
\examples{
\dontshow{if (identical(Sys.getenv("NOT_CRAN"), "true")) withAutoprint(\{ # examplesIf}
has_sqlglot()
\dontshow{\}) # examplesIf}
}
\seealso{
\code{vignette("python-integration")} for using your own Python
environment
}
