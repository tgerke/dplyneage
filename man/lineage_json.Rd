% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{lineage_json}
\alias{lineage_json}
\title{Export lineage as JSON}
\usage{
lineage_json(lineage, path = NULL, pretty = TRUE)
}
\arguments{
\item{lineage}{The result of \code{\link[=extract_lineage]{extract_lineage()}}, or any list with
\code{nodes} and \code{edges} built with \code{\link[=create_table_node]{create_table_node()}} and
\code{\link[=create_column_edge]{create_column_edge()}}.}

\item{path}{Optional file to write the JSON to. When supplied, the
string is returned invisibly.}

\item{pretty}{If \code{TRUE} (the default), indent the output for
readability. Use \code{FALSE} for a single-line document.}
}
\value{
A JSON string. With \code{metadata} (present on \code{\link[=extract_lineage]{extract_lineage()}}
results), \code{nodes} (objects with \code{id}, \code{type}, and \code{columns}), and
\code{edges} (objects with \code{source}, \code{source_column}, \code{target}, and
\code{target_column}).
}
\description{
Serializes a lineage object to a small, stable JSON document: node ids
with their columns and table type, plus one record per column-level
edge. React Flow presentation details (positions, colors) are
deliberately dropped, so the output is suitable for scripting with jq,
committing to version control (a CI diff catches accidental provenance
changes when a pipeline is edited), or feeding to a data catalog.
}
\examples{
lineage <- list(
  nodes = list(
    create_table_node("orders", c("order_id", "amount")),
    create_table_node("daily_totals", "total", table_type = "target")
  ),
  edges = list(
    create_column_edge("orders", "amount", "daily_totals", "total")
  )
)
lineage_json(lineage)

# Write to a file instead
path <- tempfile(fileext = ".json")
lineage_json(lineage, path = path)
\dontshow{if (dplyneage::has_sqlglot()) withAutoprint(\{ # examplesIf}
extract_lineage("SELECT customer_id, SUM(amount) AS total
                 FROM orders GROUP BY customer_id") |>
  lineage_json()
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=extract_lineage]{extract_lineage()}} to compute lineage automatically

Other lineage exporters:
\code{\link[=lineage_graphml]{lineage_graphml()}}
}
\concept{lineage exporters}
