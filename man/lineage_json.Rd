% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{lineage_json}
\alias{lineage_json}
\alias{lineage_from_json}
\title{Export lineage as JSON, and read it back}
\usage{
lineage_json(lineage, path = NULL, pretty = TRUE)

lineage_from_json(x)
}
\arguments{
\item{lineage}{The result of \code{\link[=extract_lineage]{extract_lineage()}}, or any list with
\code{nodes} and \code{edges} built with \code{\link[=create_table_node]{create_table_node()}} and
\code{\link[=create_column_edge]{create_column_edge()}}.}

\item{path}{Optional file to write the JSON to. When supplied, the
string is returned invisibly.}

\item{pretty}{If \code{TRUE} (the default), indent the output for
readability. Use \code{FALSE} for a single-line document.}

\item{x}{The document to read: a JSON string as \code{lineage_json()}
returns it, or the path to a file it wrote.}
}
\value{
\code{lineage_json()} returns a JSON string; see the Document shape
section. \code{lineage_from_json()} returns a lineage object (class
\code{dplyneage_lineage}) with \code{nodes}, \code{edges}, and, when the document
carries it, \code{metadata}.
}
\description{
Serializes a lineage object to a small, stable JSON document: node ids
with their columns and table type, plus one record per column-level
edge. React Flow presentation details (positions, colors) are
deliberately dropped, so the output is suitable for scripting with jq,
committing to version control (a CI diff catches accidental provenance
changes when a pipeline is edited), or feeding to a data catalog.
}
\details{
\code{lineage_from_json()} reads that document back into a lineage object,
rebuilding the nodes and edges every accessor and exporter works on.
A committed artifact can then stand in for a fresh extraction as the
\code{old} side of \code{\link[=lineage_diff]{lineage_diff()}} or \code{\link[=lineage_check]{lineage_check()}}, and lineage kept
in a catalog or a database row comes back for \code{\link[=lineage_upstream]{lineage_upstream()}}
and the exports.
}
\section{Document shape}{

The top-level keys, in order:
\itemize{
\item \code{format_version}: integer version of this document shape, currently
\enumerate{
\item It bumps only when a change would break an existing consumer;
added fields do not bump it.
}
\item \code{metadata}: present on \code{\link[=extract_lineage]{extract_lineage()}} results. Carries
\code{dialect}, \code{engine}, \code{models}, \code{node_count}, and \code{edge_count}.
\code{models} maps each model name to its \code{sql}, \code{engine}, and
\code{dialect}; a single-query extraction is a one-model map keyed by
the output table's name, so consumers read per-model SQL the same
way for both shapes. A model extracted from a table on a live
database connection also carries \code{namespace}, the OpenLineage
namespace URI inferred from that connection. The top-level
\code{dialect} and \code{engine} collapse across models, to \code{"mixed"} when
models disagree.
\item \code{nodes}: objects with \code{id}, \code{type} (\code{"source"}, \code{"transform"}, or
\code{"target"}), and \code{columns}. A node whose columns carry types (from
a live connection's schema, or a typed \code{schema} argument) adds
\code{types}, a column-to-type map over that subset; one whose columns
carry labels (\code{label} attributes on a local frame, database column
comments, or a \code{labels} argument) adds \code{labels}, shaped the same
way. Both spread along identity edges, so a passthrough column
reports its source column's type and label.
\item \code{edges}: objects with \code{source}, \code{source_column}, \code{target}, and
\code{target_column}. Edges from \code{\link[=extract_lineage]{extract_lineage()}} also carry
\code{transformation} and, on direct edges, \code{expression}. An indirect
edge whose column shapes the result in several ways (filtered and
sorted on, say) adds \code{transformations}, the full set of kinds with
the first one leading.
}

\code{lineage_from_json()} rebuilds a lineage object from these fields.
Node positions and edge labels are recomputed by the layout rules
rather than restored, and the \code{label} and \code{animated} settings of a
hand-built edge do not come back, since the document never carried
them.
}

\examples{
lineage <- list(
  nodes = list(
    create_table_node("orders", c("order_id", "amount")),
    create_table_node("daily_totals", "total", table_type = "target")
  ),
  edges = list(
    create_column_edge("orders", "amount", "daily_totals", "total")
  )
)
lineage_json(lineage)

# Write to a file instead
path <- tempfile(fileext = ".json")
lineage_json(lineage, path = path)

# Read it back: the rebuilt lineage feeds every accessor and exporter
lineage_diff(lineage, lineage_from_json(path))
\dontshow{if (identical(Sys.getenv("NOT_CRAN"), "true") && dplyneage::has_sqlglot()) withAutoprint(\{ # examplesIf}
extract_lineage("SELECT customer_id, SUM(amount) AS total
                 FROM orders GROUP BY customer_id") |>
  lineage_json()
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=extract_lineage]{extract_lineage()}} to compute lineage automatically

Other lineage exporters:
\code{\link[=lineage_emit]{lineage_emit()}},
\code{\link[=lineage_graphml]{lineage_graphml()}},
\code{\link[=lineage_mermaid]{lineage_mermaid()}},
\code{\link[=lineage_openlineage]{lineage_openlineage()}}
}
\concept{lineage exporters}
