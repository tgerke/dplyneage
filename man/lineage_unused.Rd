% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lineage_api.R
\name{lineage_unused}
\alias{lineage_unused}
\title{Report columns with no path to any target}
\usage{
lineage_unused(lineage)
}
\arguments{
\item{lineage}{The result of \code{\link[=extract_lineage]{extract_lineage()}}, or any list with
\code{nodes} and \code{edges} built with \code{\link[=create_table_node]{create_table_node()}} and
\code{\link[=create_column_edge]{create_column_edge()}}.}
}
\value{
A data frame with columns \code{table}, \code{column}, and \code{table_type},
one row per unused column, sorted by table then column. Zero rows
when every column reaches a target.
}
\description{
The dead-column report: every column on a source or transform table
from which no chain of edges reaches a target table. In a multi-model
lineage that surfaces base-table columns nothing reads and
intermediate-model outputs no downstream model consumes, both safe to
drop as far as the graph can see.
}
\details{
A single-query extraction usually reports nothing, because its source
nodes only carry columns the query referenced. Tables whose type is
unknown (hand-built nodes without a \code{table_type}) count as targets, on
the same reasoning as \code{\link[=lineage_diff]{lineage_diff()}} severity: the graph cannot see
who consumes them, so their columns, and columns feeding them, are
not called unused.
}
\examples{
lineage <- list(
  nodes = list(
    create_table_node("orders", c("order_id", "amount", "internal_note")),
    create_table_node("daily_totals", "total", table_type = "target")
  ),
  edges = list(
    create_column_edge("orders", "amount", "daily_totals", "total")
  )
)
lineage_unused(lineage)
}
\seealso{
Other lineage accessors:
\code{\link[=lineage_check]{lineage_check()}},
\code{\link[=lineage_diff]{lineage_diff()}},
\code{\link[=lineage_edges]{lineage_edges()}},
\code{\link[=lineage_tables]{lineage_tables()}},
\code{\link[=lineage_upstream]{lineage_upstream()}}
}
\concept{lineage accessors}
