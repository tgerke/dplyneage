% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{lineage_graphml}
\alias{lineage_graphml}
\title{Export lineage as GraphML}
\usage{
lineage_graphml(lineage, path = NULL)
}
\arguments{
\item{lineage}{The result of \code{\link[=extract_lineage]{extract_lineage()}}, or any list with
\code{nodes} and \code{edges} built with \code{\link[=create_table_node]{create_table_node()}} and
\code{\link[=create_column_edge]{create_column_edge()}}.}

\item{path}{Optional file to write the GraphML to. When supplied, the
string is returned invisibly.}
}
\value{
A string containing the GraphML document.
}
\description{
Serializes a lineage object to \href{http://graphml.graphdrawing.org/}{GraphML},
the XML graph format read by igraph, Gephi, yEd, and most other graph
tools. Each table column becomes a node (id \code{"table.column"}, duplicated
in a \code{name} attribute so igraph picks it up as the vertex name) with
\code{table}, \code{column}, and \code{node_type} attributes; each column-level edge
becomes a directed edge. That granularity is what makes the export
useful downstream: \code{igraph::subcomponent(g, "output.total", mode = "in")}
lists every source column feeding an output, and Gephi can color the
graph by \code{table}.
}
\examples{
lineage <- list(
  nodes = list(
    create_table_node("orders", c("order_id", "amount")),
    create_table_node("daily_totals", "total", table_type = "target")
  ),
  edges = list(
    create_column_edge("orders", "amount", "daily_totals", "total")
  )
)
cat(lineage_graphml(lineage))

# Round-trip through igraph for ancestry queries
\dontshow{if (requireNamespace("igraph", quietly = TRUE)) withAutoprint(\{ # examplesIf}
path <- tempfile(fileext = ".graphml")
lineage_graphml(lineage, path = path)
g <- igraph::read_graph(path, format = "graphml")
igraph::subcomponent(g, "daily_totals.total", mode = "in")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=extract_lineage]{extract_lineage()}} to compute lineage automatically

Other lineage exporters:
\code{\link[=lineage_json]{lineage_json()}},
\code{\link[=lineage_mermaid]{lineage_mermaid()}},
\code{\link[=lineage_openlineage]{lineage_openlineage()}}
}
\concept{lineage exporters}
