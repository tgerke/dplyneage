% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lineage_api.R
\name{lineage_check}
\alias{lineage_check}
\title{Gate a CI run on lineage changes}
\usage{
lineage_check(
  old,
  new,
  fail_on = c("breaking", "any", "none"),
  annotate = NULL
)
}
\arguments{
\item{old, new}{Lineage objects from \code{\link[=extract_lineage]{extract_lineage()}} (or lists
with \code{nodes} and \code{edges}), in before/after order — typically main's
extraction and the branch's.}

\item{fail_on}{Which changes fail the check: \code{"breaking"} (the
default) stops on breaking changes only, \code{"any"} stops on any
change, and \code{"none"} always passes, printing findings as a report.}

\item{annotate}{Print findings as GitHub Actions workflow commands
(\verb{::error::}/\verb{::warning::}) instead of plain lines. The default
\code{NULL} turns annotations on exactly when the \code{GITHUB_ACTIONS}
environment variable is \code{"true"}, as it is on every Actions runner.}
}
\value{
The \code{\link[=lineage_diff]{lineage_diff()}} of the two lineages, invisibly. When
the check fails, the error condition (class
\code{"dplyneage_lineage_check_failure"}) carries the same diff in its
\code{diff} field.
}
\description{
Compares two lineage extractions and fails when the changes cross a
severity threshold: the one-call version of "diff the lineage and
stop the merge". Findings print one per line, and on GitHub Actions
they become \verb{::error}/\verb{::warning} annotations on the run.
}
\details{
The failure is a classed condition, so a wrapper can catch it and
report its own way:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{tryCatch(
  lineage_check(old, new),
  dplyneage_lineage_check_failure = function(cnd) post_comment(cnd$diff)
)
}\if{html}{\out{</div>}}

\code{vignette("articles/lineage-ci")} on the package site walks through
the full setup, including a GitHub Actions job that extracts lineage
on a pull request branch and on main, then fails the merge on
breaking changes.
}
\examples{
old <- list(
  nodes = list(
    create_table_node("orders", "amount"),
    create_table_node("out", "total", table_type = "target")
  ),
  edges = list(create_column_edge("orders", "amount", "out", "total"))
)
# an unchanged lineage passes
lineage_check(old, old, annotate = FALSE)

# removing the edge into a target column is breaking
new <- old
new$edges <- list()
try(lineage_check(old, new, annotate = FALSE))

# report-only mode never fails
lineage_check(old, new, fail_on = "none", annotate = FALSE)
}
\seealso{
Other lineage accessors:
\code{\link[=lineage_diff]{lineage_diff()}},
\code{\link[=lineage_edges]{lineage_edges()}},
\code{\link[=lineage_tables]{lineage_tables()}},
\code{\link[=lineage_upstream]{lineage_upstream()}}
}
\concept{lineage accessors}
