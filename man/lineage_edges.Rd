% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lineage_api.R
\name{lineage_edges}
\alias{lineage_edges}
\title{Lineage edges as a data frame}
\usage{
lineage_edges(lineage)
}
\arguments{
\item{lineage}{The result of \code{\link[=extract_lineage]{extract_lineage()}}, or any list with
\code{nodes} and \code{edges} built with \code{\link[=create_table_node]{create_table_node()}} and
\code{\link[=create_column_edge]{create_column_edge()}}.}
}
\value{
A data frame with columns \code{source_table}, \code{source_column},
\code{target_table}, \code{target_column}, \code{transformation}, and \code{expression}.
}
\description{
Flattens a lineage object's column-level edges into one row per edge,
for filtering, joining, and summarising with ordinary data frame tools.
For edges produced by \code{\link[=extract_lineage]{extract_lineage()}}, the \code{transformation} column
classifies each edge (\code{"identity"} for plain column passthrough,
\code{"aggregation"}, or \code{"transformation"}) and \code{expression} records the
output column's defining expression; both are \code{NA} for hand-built
edges.
}
\examples{
lineage <- list(
  nodes = list(
    create_table_node("orders", c("order_id", "amount")),
    create_table_node("daily_totals", "total", table_type = "target")
  ),
  edges = list(
    create_column_edge("orders", "amount", "daily_totals", "total")
  )
)
lineage_edges(lineage)
}
\seealso{
Other lineage accessors:
\code{\link[=lineage_diff]{lineage_diff()}},
\code{\link[=lineage_tables]{lineage_tables()}},
\code{\link[=lineage_upstream]{lineage_upstream()}}
}
\concept{lineage accessors}
