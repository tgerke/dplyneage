% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{lineage_mermaid}
\alias{lineage_mermaid}
\title{Export lineage as a Mermaid flowchart}
\usage{
lineage_mermaid(lineage, path = NULL)
}
\arguments{
\item{lineage}{The result of \code{\link[=extract_lineage]{extract_lineage()}}, or any list with
\code{nodes} and \code{edges} built with \code{\link[=create_table_node]{create_table_node()}} and
\code{\link[=create_column_edge]{create_column_edge()}}.}

\item{path}{Optional file to write the Mermaid text to. When supplied,
the string is returned invisibly.}
}
\value{
A string containing the Mermaid flowchart definition.
}
\description{
Serializes a lineage object to \href{https://mermaid.js.org/}{Mermaid}
flowchart text. Mermaid renders natively in GitHub markdown, Quarto,
and most documentation tools, so this is the exporter to reach for when
lineage should live \emph{in the docs}: paste the output into a
\verb{```mermaid} code fence and the diagram renders with no R, no
htmlwidget, and no JavaScript bundle.
}
\details{
Each table becomes a subgraph containing its columns, colored by table
type with the same palette as \code{\link[=lineage_flow]{lineage_flow()}}. Non-identity edges are
labeled with the column's defining expression, and indirect edges
(from \code{extract_lineage(include_indirect = TRUE)}) draw dashed.
}
\examples{
lineage <- list(
  nodes = list(
    create_table_node("orders", c("order_id", "amount")),
    create_table_node("daily_totals", "total", table_type = "target")
  ),
  edges = list(
    create_column_edge("orders", "amount", "daily_totals", "total")
  )
)
cat(lineage_mermaid(lineage))

# Ready to paste into a GitHub README or Quarto document:
cat("```mermaid\n", lineage_mermaid(lineage), "```\n", sep = "")
}
\seealso{
\code{\link[=extract_lineage]{extract_lineage()}} to compute lineage automatically

Other lineage exporters:
\code{\link[=lineage_graphml]{lineage_graphml()}},
\code{\link[=lineage_json]{lineage_json()}},
\code{\link[=lineage_openlineage]{lineage_openlineage()}}
}
\concept{lineage exporters}
