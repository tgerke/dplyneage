% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emit.R
\name{lineage_emit}
\alias{lineage_emit}
\title{Send OpenLineage events to a backend over HTTP}
\usage{
lineage_emit(
  lineage,
  url = NULL,
  events = "run",
  namespace = NULL,
  job_name = "extract_lineage",
  run_id = NULL,
  event_time = NULL,
  event_type = "COMPLETE",
  output_name = NULL,
  nominal_time = NULL,
  parent = NULL,
  api_key = NULL,
  headers = NULL,
  endpoint = "api/v1/lineage"
)
}
\arguments{
\item{lineage}{The result of \code{\link[=extract_lineage]{extract_lineage()}}, or any list with
\code{nodes} and \code{edges} built with \code{\link[=create_table_node]{create_table_node()}} and
\code{\link[=create_column_edge]{create_column_edge()}}.}

\item{url}{Base URL of the OpenLineage backend, e.g.
\code{"http://localhost:5000"} for a local Marquez. Defaults to the
\code{OPENLINEAGE_URL} environment variable, the same one the reference
clients read.}

\item{events}{Which event kinds to emit: any combination of \code{"run"}
(the default; one \code{RunEvent} for the whole extraction), \code{"job"}
(run-less \code{JobEvent}s, one per model), and \code{"dataset"} (run-less
\code{DatasetEvent}s, one per dataset). See the Static lineage events
section.}

\item{namespace}{Dataset and job namespace recorded in the event. The
default \code{NULL} resolves each dataset's namespace from the connection
captured at extraction time (see the Namespaces section); pass a
string to override everything, matching your catalog's namespace
when integrating.}

\item{job_name}{Name recorded for the job that produced this lineage.}

\item{run_id}{UUID identifying the run. Generated when \code{NULL} (the
default); pass a fixed UUID for reproducible output.}

\item{event_time}{Event timestamp in ISO-8601 format. The current UTC
time when \code{NULL} (the default); pass a fixed timestamp for
reproducible output.}

\item{event_type}{The run state a \code{"run"} event reports:
\code{"COMPLETE"} (the default), \code{"START"}, \code{"RUNNING"}, \code{"ABORT"},
\code{"FAIL"}, or \code{"OTHER"}. Static events carry no run state.}

\item{output_name}{Name recorded for the output dataset in place of
the synthetic \code{"output"} node id of a single-query extraction; use
it when the query's result lands in a known table. Errors on
multi-model lineage, whose models already carry their real names.}

\item{nominal_time}{One or two ISO-8601 timestamps (the scheduled
\code{nominalStartTime} and optionally \code{nominalEndTime}) emitted as the
\code{nominalTime} run facet. \code{NULL} (the default) omits the facet.}

\item{parent}{A \code{list(run_id = , job_name = )}, optionally with a
\code{namespace}, identifying the orchestrating run this event belongs
under (an Airflow task, a dbt run); emitted as the \code{parent} run
facet. \code{NULL} (the default) omits the facet.}

\item{api_key}{Bearer token added as an \code{Authorization} header.
Defaults to the \code{OPENLINEAGE_API_KEY} environment variable; unset
means no auth header.}

\item{headers}{A named character vector or list of extra HTTP
headers.}

\item{endpoint}{Path of the lineage endpoint under \code{url}.}
}
\value{
Invisibly, a data frame with one row per event sent: \code{event}
(index) and \code{status} (HTTP status code).
}
\description{
Builds the same events as \code{\link[=lineage_openlineage]{lineage_openlineage()}} and POSTs each one
to an OpenLineage endpoint, so dplyneage-extracted lineage lands in a
running catalog (Marquez, DataHub, or anything else that speaks the
protocol) next to lineage from dbt, Airflow, or Spark. One request
per event, JSON body, matching how the reference OpenLineage clients
transport events.
}
\details{
The default endpoint path \code{api/v1/lineage} is where Marquez listens;
for other backends set \code{endpoint} (DataHub, for example, exposes the
OpenLineage endpoint under its own prefix). Marquez accepts run
events and the static \code{events = "job"} / \code{events = "dataset"} kinds
alike.
}
\section{Errors}{

A failed request (connection refused, or an HTTP error status)
stops with a condition of class \code{dplyneage_emit_failure} carrying
\code{event} (the failing index), \code{status} (the HTTP status, \code{NA} when the
request never got a response), and \code{results} (the rows for events
already sent). Events after the failing one are not sent.
}

\examples{
\dontrun{
lineage <- extract_lineage(my_query)

# A local Marquez quickstart listens on port 5000
lineage_emit(lineage, url = "http://localhost:5000")

# Design-time lineage, no fabricated run, nested under a known job
lineage_emit(lineage, url = "http://localhost:5000", events = "job")
}
}
\seealso{
\code{\link[=lineage_openlineage]{lineage_openlineage()}} for the event documents themselves

Other lineage exporters:
\code{\link[=lineage_graphml]{lineage_graphml()}},
\code{\link[=lineage_json]{lineage_json()}},
\code{\link[=lineage_mermaid]{lineage_mermaid()}},
\code{\link[=lineage_openlineage]{lineage_openlineage()}}
}
\concept{lineage exporters}
